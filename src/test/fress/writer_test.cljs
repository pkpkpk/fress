(ns fress.writer-test
  (:require [cljs.test :refer-macros [deftest is testing]]
            [fress.raw-output :as rawOut]
            [fress.codes :as codes]
            [fress.writer :as writer]))

(deftest writeBytes-test
  (let []

    ))